import { Button, Alert, Card } from "react-bootstrap";
import Layout from "./Layout";

const Dashboard = () => {
  return (
    <Layout>
      <div className="text-center">
        <Button>ich möchte mich am verkauf beteiligen</Button>
      </div>
    </Layout>
  );
};

export default Dashboard;
